pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm lint
    test:
      run: pnpm test

pre-push:
  parallel: false
  commands:
    check:
      run: ./scripts/check
    typecheck:
      run: tsc --noEmit
    site-check:
      run: pnpm --filter site astro check
    build:
      run: pnpm build
    publint:
      run: pnpm check:exports
